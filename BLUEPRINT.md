# TANGLE (하루 한 매듭) 블루프린트

## 한 줄 정의
매일 같은 "엉킨 매듭" 하나가 출제되고(Wordle식), 노드를 끌어 **선이 하나도 안 겹치게** 풀면 끝 — 무료·설치 없이 브라우저에서, 결과를 링크로 공유해 대결하는 데일리 퍼즐.

## 가정 (사용자가 뒤집을 수 있음)
- 원안은 "지혜의 고리(3D 캐스트 퍼즐)"였으나, **진짜 3D 분리 퍼즐을 '항상 풀리게' 절차 생성하는 건 연구급 난제**다. 그래서 MVP는 같은 "엉킨 걸 푼다"는 아하-경험을 **수학적으로 반드시 풀리는** 형태로 증류한 **매듭풀기(Planarity/untangle)**로 만든다. 시작 배치가 평면 그래프라 **겹침 0 해가 존재함이 보장**된다.
- **3D 프레젠테이션**(노드를 3D로 모델링/회전)은 로드맵. MVP는 2D 캔버스(모바일 플레이·의존성0 우선).
- 데일리: 날짜에서 시드 → 전원 동일 문제. `?seed=`로 연습판 무제한.
- 백엔드 0(서버·계정 없음). 배포는 GitHub Pages(vs-arena·camo와 동일).

## 시장성 & 경쟁
- **Wordle 파생 데일리 퍼즐**은 검증된 바이럴 포맷(전원 같은 문제 + 결과 공유). 직접 경쟁 "데일리 untangle"은 희소.
- 원조 **Planarity**(John Tantalo)는 대히트했으나 *데일리+공유* 루프가 없음 → 그 지점이 우리 차별.
- 지혜의 고리/캐스트 퍼즐은 물리 제품·앱 다수지만 **매일·무료·웹·공유**형은 공백.

## BM 매칭
- **광고 → 규모 후 코스메틱**(매듭 테마/색). 근거: 데일리 리텐션이 강한 캐주얼 = 광고 인벤토리. vs-arena 애드슬롯 패턴 재사용. 과금 시점: DAU 확보 후.

## 보안 체크리스트 (해당만)
- 시크릿 0(백엔드 없음). `?seed`는 **정수 파싱+범위 clamp**로만 사용.
- 동적 DOM(점수/공유문구)은 `textContent`로만 — `innerHTML` 값 보간 금지(SEC-2, coding-rules 게이트).
- HTTPS(Pages 기본).

## 기술스택
- 바닐라 JS + Canvas 2D, 의존성 0, GitHub Pages. 시드 RNG=mulberry32. 절차적 WebAudio.
- 근거: 사용자 역량·기존 게임과 동일 스택, 그래프 생성/교차판정은 순수 JS로 충분.

## 활용 스킬
- **verify/preview — Phase 2**: 브라우저 구동 + 생성 퍼즐이 **실제로 풀리는지**(교차 0 도달 가능) 알고리즘 검증.

## MVP 범위
**IN**: 데일리 시드로 **반드시 풀리는** 평면그래프 생성 → 스크램블, 노드 드래그, 실시간 교차수 표시(겹치는 선 빨강/해결 초록), 승리 판정(교차 0), 이동수·시간, 공유(데일리#+기록), 연습판(`?seed`), 사운드, 모바일 대응.
**OUT(로드맵)**: 3D 프레젠테이션, 난이도 곡선/일일 랭킹 서버, 힌트, 코스메틱, 진짜 캐스트퍼즐 모드.

## 로드맵
1. 일일 랭킹/스트릭 + 공유 카드(og:image)
2. 난이도 티어(노드/엣지 수) + 힌트 + 무늬/테마 코스메틱
3. **3D 프레젠테이션**(Three.js: 노드 3D 회전) — 원안의 "3D 모델링" 감성 복원
